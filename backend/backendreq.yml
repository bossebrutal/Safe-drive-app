name: safedrive
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - uvicorn
  - pip
  - pip:
      - fastapi
      - sqlalchemy
      - mysql-connector-python
      - passlib[bcrypt]